{
  "name": "gradient-bot",
  "version": "1.0.0",
  "description": "Gradient network bot",
  "main": "app.js",
  "scripts": {
    "start": "node start.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "console-stamp": "^3.1.1",
    "dotenv": "^16.3.1",
    "pm2": "^5.3.0"
  }
}
